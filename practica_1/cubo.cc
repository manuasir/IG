#ifdef _WIN32
#include <windows.h>
#endif
#include <cubo.h>

Cubo::Cubo(float tam){	
	createArrayData(tam);
}
void Cubo::createArrayData(float tam){
	// Vertices
	/*puntos.push_back(_vertex3f(-tam,	-tam, 	tam));
	puntos.push_back(_vertex3f(tam,	-tam, 	tam));
	puntos.push_back(_vertex3f(tam, 	tam, 	tam));
	puntos.push_back(_vertex3f(-tam, 	tam, 	tam));
    puntos.push_back(_vertex3f(-tam,	-tam,	 -tam));
    puntos.push_back(_vertex3f(tam,	-tam,	 -tam));
    puntos.push_back(_vertex3f(tam,	 tam,	 -tam));
    puntos.push_back(_vertex3f(-tam,	 tam,	 -tam));*/
}

